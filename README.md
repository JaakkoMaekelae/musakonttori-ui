# @musakonttori/ui

Shared Musakonttori UI components.

## Usage

```tsx
import { Button, Badge } from "@musakonttori/ui";
```

## Components

- **Button** — Primary, secondary, ghost, and destructive variants with size options.
- **Badge** — Status badges with tone colors (neutral, brand, success, warning, error, info).
